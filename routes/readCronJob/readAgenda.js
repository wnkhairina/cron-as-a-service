const Job = require('../../schema/job');

const readAgenda = async (req, res, job) => {
    const jobs = await agenda.jobs({name: 'Ping website'}, function(err,jobs){
        
    });
};

module.exports = readAgenda